name: second work flow
on: workflow_dispatch
jobs:
  first job:
   runs-on: ubuntu-latest
   steps:
     - name: Hello World
       run: | 
              echo "Hello World"
              echo "Bye World"
